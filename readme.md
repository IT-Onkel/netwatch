# netwatch

Internet quality evidence collector (Ping/Jitter/Loss, DNS, MTR, UDP iperf, Speedtest) with 24h default runtime.

## Install (Debian)
```bash
sudo bash install.sh


## One-liner install (from latest GitHub release)

curl -fsSL https://github.com/IT-Onkel/netwatch/releases/latest/download/netwatch-install.sh | sudo bash
